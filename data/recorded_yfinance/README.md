# Recorded yfinance fixtures

These JSON files are yfinance-shaped fixtures for offline tests. They are
marked `SYNTHETIC_RECORDED_YFINANCE_SHAPE`; they are not represented as live
market data. Live tests are opt-in via `SWS_RUN_LIVE_TESTS=1`.
