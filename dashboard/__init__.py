"""Streamlit dashboard package for SWS Snowflake Engine v3.1.

The dashboard is intentionally a UI/client layer over the FastAPI backend. It
must not import or call the engine or the SQLite persistence layer directly.
"""
